apiVersion: v1
kind: Service
metadata:
  name: multicloud-app-service
spec:
  type: NodePort
  selector:
    app: multicloud-app
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30000
